import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Fraud Detection System | VexStorm\'26',
  description: 'Real-time transaction fraud detection and risk assessment powered by AI',
  keywords: ['fraud detection', 'fintech', 'transaction monitoring', 'risk assessment'],
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
}
